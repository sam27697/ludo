// Minimal entry point, proving the server package builds and runs. No
// listener, no room logic yet -- that lands in its own order.
void main() {
  print('ludo_server: scaffold only, nothing listening yet');
}
